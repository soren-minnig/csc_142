/*
int get_year in 2-20 could be marked as const because
it won't affect the object, and it only returns a value

Any explicit type can be swapped with auto, but it is
especially useful for typings with long names
*/